<% @messages.each do |message| %>
  <h3><%= message.title.capitalize %></h3>
  <%= link_to "View Message", message_path(message), class:"btn btn-info" %>

<% end  %>


<%= link_to "New Message", new_message_path %>
